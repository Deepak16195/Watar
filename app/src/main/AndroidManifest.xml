<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.watar.soft.watar">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".activity.InitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        >
        <activity android:name=".activity.First_page"
            android:launchMode="singleInstance"
            >
               <intent-filter>
                     <action android:name="android.intent.action.MAIN"
                         />

                     <category android:name="android.intent.category.LAUNCHER"
                         />
                 </intent-filter>
             </activity>
             <activity android:name=".activity.MainActivity"
                 android:launchMode="singleInstance">
           <!--<intent-filter>
                 <action android:name="android.intent.action.MAIN" />

                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
-->    </activity>


        <activity android:name=".activity.Login_page"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.Sing_up"
            android:launchMode="singleInstance"/>
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme"
            android:launchMode="singleInstance"/>
        <activity
            android:name=".activity.demo"
            android:label="@string/title_activity_demo"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.AlbumsActivity"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.DayNightActivity"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.MusicPlay"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.ArtistDeatils"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.Albums"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.ForgotPassword"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.ResetPassword"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.Queuelist"
            android:launchMode="singleInstance"/>
    </application>

</manifest>